# Force LF for shell scripts so they run on macOS / Linux
*.sh    text eol=lf

# Force CRLF for Windows batch files
*.bat   text eol=crlf
*.cmd   text eol=crlf

# Default: normalize text, store LF in the repo
*       text=auto
